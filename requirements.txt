# Core dependencies
PySide6==6.8.0
pyserial==3.5
keyboard==0.13.5
configparser==6.0.1

# Optional Windows dependencies (for setup.py shortcuts)
# Install with: pip install winshell pywin32
# winshell==0.6
# pywin32==306
